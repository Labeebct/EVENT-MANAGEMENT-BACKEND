exports.postSignup = (req,res) => { 
    try {
        
    } catch (error) {
        console.log('Error in admin post signup',error);
        res.status(500).send('internal server error')
    }
}
exports.postLogin = (req,res) => {
    try {
        
    } catch (error) {
        console.log('Error in admin post signup',error);
        res.status(500).send('internal server error')
    }
}